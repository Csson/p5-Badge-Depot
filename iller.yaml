---
+doctype: dist
name: Badge-Depot

+prereqs:
  runtime:
    requires:
     - perl: 5.010001
     - Moose::Role: 2.0000
     - Types::Standard
     - Types::URI
  test:
    requires:
      - Moose: 2.0000

+plugins:
 - +config: Author::CSSON

 - +add_plugin: TravisYML
   +before: Git::Tag
   perl_version: 5.10.1 5.14 5.20

---
+doctype: weaver

+plugins:
 - +config: Author::CSSON

 - +remove_plugin: Splint
